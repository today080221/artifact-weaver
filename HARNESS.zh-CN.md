# ArtifactWeaver 执行护栏体系

执行护栏体系用于保证 ArtifactWeaver 适合公开开源协作。它保护公开渲染契约、双语文档、假数据示例和私有数据边界。

## 开始协议

每次开始工作前，执行者必须：

1. 读取本文档。
2. 读取当前 issue 或任务描述。
3. 读取对应上下文包。
4. 确认允许修改的路径。
5. 确认范围外路径。
6. 运行 `git status --short --branch -uall`。
7. 确认当前分支。
8. 在任务需要时，向变更日志和决策日志添加开始记录。
9. 不触碰上下文包之外的文件；如确实需要，必须先记录理由。

## 完成协议

交接前，执行者必须：

1. 更新英文和简体中文文档。
2. 更新变更日志。
3. 如有架构选择，更新决策日志。
4. 如有新增或修改命令，更新快速开始或 CLI 参考。
5. 如有渲染清单变化，更新 schema 文档。
6. 运行验证。
7. 输出交接报告。
8. 不自动提交。
9. 不自动推送。

## M0 边界

M0 必须保持 Python-only。不要引入 Node、Bun、Vite、React、Vue、Slidev、Playwright、PDF 导出、CI 工作流、PyPI/npm 发布、Unity 集成、飞书集成、Jenkins 集成、Steam 集成或 NoTimeToDie 专用适配器。

示例必须保持假数据。生成的 `dist/` 输出不能进入 Git。
