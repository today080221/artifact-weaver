# 执行者协议

## 开始

1. 读取 `HARNESS.zh-CN.md`。
2. 读取 issue 或任务。
3. 读取对应上下文包。
4. 确认允许修改的路径和范围外路径。
5. 运行 `git status --short --branch -uall`。
6. 在任务需要时记录开始条目。

## 工作中

保持在上下文包范围内。如需扩大范围，必须先记录理由，再触碰额外文件。

## 完成

运行验证，更新文档，更新日志，并在不自动提交、不自动推送的前提下交接。
